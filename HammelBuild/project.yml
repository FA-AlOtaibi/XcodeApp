name: Hammel
options:
  bundleIdPrefix: com.fahad
  deploymentTarget:
    iOS: "17.0"
settings:
  base:
    SWIFT_VERSION: 5.0
    IPHONEOS_DEPLOYMENT_TARGET: 17.0
targets:
  Hammel:
    type: application
    platform: iOS
    sources:
      - Sources
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.fahad.Hammel
        PRODUCT_NAME: Hammel
        GENERATE_INFOPLIST_FILE: YES
        INFOPLIST_KEY_CFBundleDisplayName: "حمّل"
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        INFOPLIST_KEY_NSPhotoLibraryAddUsageDescription: "يحتاج حمّل إلى إذن لإضافة الفيديوهات والصور التي تختار حفظها إلى مكتبة الصور."
        INFOPLIST_KEY_NSPhotoLibraryUsageDescription: "يستخدم حمّل مكتبة الصور فقط عند اختيار حفظ أو مشاركة الوسائط."
        TARGETED_DEVICE_FAMILY: "1"
        CODE_SIGN_STYLE: Automatic
