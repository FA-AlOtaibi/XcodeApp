name: Object Studio IPA

on:
  push:
    branches: [ object-studio ]
    paths:
      - 'ObjectStudio/**'
      - '.github/workflows/object-studio-ipa.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Install XcodeGen
        run: brew install xcodegen
      - name: Generate Xcode project
        working-directory: ObjectStudio
        run: xcodegen generate
      - name: Test network regressions on simulator
        working-directory: ObjectStudio
        run: |
          set -o pipefail
          DEVICE_ID=$(xcrun simctl list devices available -j | python3 -c 'import json,sys; d=json.load(sys.stdin); print(next(v["udid"] for k,vs in d["devices"].items() if "iOS" in k for v in vs if "iPhone" in v["name"]))')
          xcodebuild -project ObjectStudio.xcodeproj -scheme ObjectStudio \
            -destination "platform=iOS Simulator,id=$DEVICE_ID" \
            -parallel-testing-enabled NO CODE_SIGNING_ALLOWED=NO test
      - name: Build unsigned app
        working-directory: ObjectStudio
        run: |
          set -o pipefail
          xcodebuild \
            -project ObjectStudio.xcodeproj \
            -scheme ObjectStudio \
            -configuration Release \
            -sdk iphoneos \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY='' \
            build
      - name: Package IPA
        working-directory: ObjectStudio
        run: |
          APP=$(find build/Build/Products/Release-iphoneos -maxdepth 1 -name '*.app' | head -1)
          test -n "$APP"
          mkdir -p Payload
          cp -R "$APP" Payload/
          zip -qry ObjectStudio.ipa Payload
      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: ObjectStudio-IPA
          path: ObjectStudio/ObjectStudio.ipa
          if-no-files-found: error
